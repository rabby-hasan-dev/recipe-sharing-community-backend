
export enum UserRole {
    SUPER_ADMIN = 'superAdmin',
    ADMIN = 'admin',
    USER = 'user',
}

export enum UserStatus {
    ACTIVE = 'active',
    IN_PROGRESS = 'in-progress',
    BLOCKED = 'blocked',
}

